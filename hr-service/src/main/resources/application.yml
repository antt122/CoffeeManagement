server:
  port: 8081 # 👈 Port mặc định cho hr-service

spring:
  # Kích hoạt profile "local" làm mặc định
  # (Sẽ tự động nạp file "application-local.yml")
  profiles:
    active: local

  # 3. JPA/Hibernate (Cấu hình chung)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# 7. Actuator (Cấu hình chung)
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always